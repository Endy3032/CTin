#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll t, n, x, y;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);

	cin >> t;
	while (t--) {
		cin >> n;

		queue<pair<ll, ll>> q;

		for (int i = 0; i < n; i++) {
			cin >> x >> y;
			q.push({x, y});
		}

	}
}