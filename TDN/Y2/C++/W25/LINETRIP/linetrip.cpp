#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll n, x, mn, mx;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);

	cin >> n;
	while (n--) {
		cin
	}
}