#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
ll n;

int main() {
	ifstream cin("DP_A6.inp");
	ofstream cout("DP_A6.out");

	ios_base::sync_with_stdio(false);
	std::cin.rdbuf(cin.rdbuf());
	std::cout.rdbuf(cout.rdbuf());
}
